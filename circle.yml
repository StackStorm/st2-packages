version: 2
executorType: machine
stages:
  build:
    workDir: ~/st2-packages
    steps:
      - type: checkout
      - type: shell
        name: Verify Docker Works
        command: |
          docker --version
          docker run hello-world
