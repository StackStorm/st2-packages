image: quay.io/dennybaa/droneunit-ubuntu
script:
  - cat /etc/hosts
  - pwd
  - find ./
  - "/root/.ssh/ && chmod 700 /root/.ssh"
  - "cp conf/sefella /home/sefella/.ssh/id_rsa && chmod 600 /home/sefella/.ssh/id_rsa"
services:
  - quay.io/stackstorm/st2packages-buildenv-deb
