# Defaults for st2notifier initscript
# sourced by /etc/init.d/st2notifier
# installed at /etc/default/st2notifier by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Change default service startup arguments
#DAEMON_ARGS="--config-file /etc/st2/st2.conf"
