# Defaults for st2actionrunner initscript
# sourced by /etc/init.d/st2actionrunner
# installed at /etc/default/st2actionrunner by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Change default service startup arguments
#DAEMON_ARGS="--config-file /etc/st2/st2.conf"
