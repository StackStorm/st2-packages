# Defaults for st2exporter initscript
# sourced by /etc/init.d/st2exporter
# installed at /etc/default/st2exporter by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Change default service startup arguments
#DAEMON_ARGS="--config-file /etc/st2/st2.conf"
