# Defaults for st2auth initscript
# sourced by /etc/init.d/st2auth
# installed at /etc/default/st2auth by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Change default service startup arguments
#DAEMON_ARGS="--config-file /etc/st2/st2.conf"
